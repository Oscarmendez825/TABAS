export interface MaletaModel {
    numero_maleta:number,
    cedulaUsuario:number,
    bagcartId: number,
    numVuelo: number,
    peso:number,
    color:string,
    aceptada:boolean,
    costo:number,
    scanId:number
}
